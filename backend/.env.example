# MongoDB Configuration
# For local development, use: mongodb://localhost:27017/airtable-form-builder
# For production, use MongoDB Atlas connection string
MONGODB_URI=mongodb://localhost:27017/airtable-form-builder

# Server Configuration
PORT=5000
NODE_ENV=development

# Airtable OAuth Configuration
# Get these from: https://airtable.com/create/oauth
# 1. Create a new OAuth integration
# 2. Generate a client secret
# 3. Add redirect URI: http://localhost:5000/auth/airtable/callback (development)
#    For production, use: https://your-backend-url.com/auth/airtable/callback
AIRTABLE_CLIENT_ID=your_airtable_client_id_here
AIRTABLE_CLIENT_SECRET=your_airtable_client_secret_here
AIRTABLE_REDIRECT_URI=http://localhost:5000/auth/airtable/callback

# Frontend URL
# Development: http://localhost:3000
# Production: https://your-frontend-url.vercel.app
FRONTEND_URL=http://localhost:3000

# Session Secret
# Generate a random string for session encryption
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=generate_a_random_64_character_string_here

# Airtable Webhook Configuration
# Generate a secure random string for webhook signature verification
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AIRTABLE_WEBHOOK_SECRET=generate_a_random_webhook_secret_here
